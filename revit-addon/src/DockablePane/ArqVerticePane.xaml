<UserControl x:Class="ArqVertice.Revit.DockablePane.ArqVerticePane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="#0D1117" Foreground="#E6EDF3" FontFamily="Segoe UI">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#161B22" CornerRadius="6" Padding="10" Margin="0,0,0,10" BorderBrush="#30363D" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="ARQVÉRTICE STUDIO" FontWeight="Bold" FontSize="14" Foreground="#58A6FF"/>
                <TextBlock Text="BIM Platform Connector • v1.0.0" FontSize="10" Foreground="#8B949E"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Ellipse Width="8" Height="8" Fill="#238636" Margin="0,0,6,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Conectado (127.0.0.1:4848)" FontSize="11" Foreground="#7EE787"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Contexto do Documento Ativo -->
        <Border Grid.Row="1" Background="#161B22" CornerRadius="6" Padding="10" Margin="0,0,0,10" BorderBrush="#30363D" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="DOCUMENTO ATIVO" FontWeight="SemiBold" FontSize="11" Foreground="#8B949E"/>
                <TextBlock x:Name="TxtDocTitle" Text="Residência_Villa_Verde.rvt" FontSize="12" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtSelectionCount" Text="0 elementos selecionados" FontSize="11" Foreground="#58A6FF" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Ações Rápidas de Sincronização -->
        <Border Grid.Row="2" Background="#161B22" CornerRadius="6" Padding="10" Margin="0,0,0,10" BorderBrush="#30363D" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="AÇÕES BIM &amp; IA" FontWeight="SemiBold" FontSize="11" Foreground="#8B949E" Margin="0,0,0,6"/>
                <Button Content="Sincronizar com Studio" Height="28" Background="#238636" Foreground="White" BorderThickness="0" Margin="0,0,0,6" Click="BtnSync_Click"/>
                <Button Content="Inspecionar Seleção com IA" Height="28" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" BorderThickness="1" Margin="0,0,0,6" Click="BtnInspect_Click"/>
                <Button Content="Auditar Conformidade NBR" Height="28" Background="#21262D" Foreground="#E6EDF3" BorderBrush="#30363D" BorderThickness="1" Click="BtnAudit_Click"/>
            </StackPanel>
        </Border>

        <!-- Logs de Eventos -->
        <Border Grid.Row="3" Background="#010409" CornerRadius="6" Padding="8" BorderBrush="#30363D" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="TxtLogs" Text="[Ponte Inicializada] Aguardando requisições do ArqVértice Studio..." FontFamily="Consolas" FontSize="10" Foreground="#8B949E" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Footer -->
        <TextBlock Grid.Row="4" Text="Loopback Seguro • Porta 4848" FontSize="9" Foreground="#484F58" HorizontalAlignment="Center" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
