# pyRevit

pyRevit e opcional e fica restrito a utilitarios, comandos rapidos, batch scripts, diagnosticos e helpers de desenvolvimento. Nao e o canal principal de sincronizacao.

A compatibilidade deve ser verificada contra versao/build do Revit e engine instalado.
