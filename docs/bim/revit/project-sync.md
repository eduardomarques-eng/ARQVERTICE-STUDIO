# Project Sync

J40 compara snapshots por `source`, `sourceId`, `revitUniqueId`, `version`, geometria, transform, tipo, familia, material, parametros e visibilidade.

Conflitos mostram Revit, ArqVertice e ultimo snapshot. Nenhuma alteracao e sobrescrita sem ChangeSet, aprovacao e auditoria.
